@{
    ViewBag.Title = "Home Page";
}

@if (!User.Identity.IsAuthenticated) {
    <div class="jumbotron text-center">
        <img src="~/images/4tugafinal.png"height="300" width="300" />
        <hr>
        <p>
            @Html.ActionLink("Entrar", "Login", "Account", null, new { @class = "btn btn-primary btn-lg" })
            ou
            @Html.ActionLink("Registar", "Register", "Account")
        </p>
    </div>
}
else
{
    <div class="jumbotron text-center">
        <h1><img src="~/images/4tuga_semfundo.png" width="300" height="300" /></h1>
    </div>
}